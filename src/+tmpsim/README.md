# src/+tmpsim

放置 MATLAB System、MATLAB Function 和在线闭环工具函数。

计划模块包括 ReferenceManager、ErrorFeatureBuilder、HistoryBuffer、TransformerRiskSystem、RiskSupervisor、LateralMpcSystem、SpeedOuterLoop 和 CarSimAdapter。
